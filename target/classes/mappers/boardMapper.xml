<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.findpet119.common.board.mappers.board">

	<!-- 게시물 목록 -->
	<select id="list" resultType="com.my.findpet119.common.board.service.BoardVO">
		select
			seq, title, content,  hit_cnt, create_userid, create_datetime
			from board
	</select>
	
	<!-- 게시물 작성 -->
	<insert id="write" parameterType="com.my.findpet119.common.board.service.BoardVO">
		insert into board(seq, category, title, content, hit_cnt,
		create_userid, create_datetime,	delete_flag) values 
		 (board_seq.nextval,'1', #{title}, #{content}, '0', 
		#{create_userid}, sysdate, 'N')
	</insert>
	
	<!-- 게시물 조회 -->
	<select id="view" parameterType="int" resultType="com.my.findpet119.common.board.service.BoardVO">
		select 
			seq title, content, create_userid
		from 
			board
		where
			seq = #{seq}			
	</select>

	<!-- 게시물 수정 -->
	<update id="modify" parameterType="com.my.findpet119.common.board.service.BoardVO" >
		update board
			set
				title = #{title},
				content = #{content},
				create_userid = #{create_userid}
			where seq = #{seq}
	</update>
	
	<!-- 게시물 삭제 -->
	<delete id="delete" parameterType="int">
		delete from board
		where seq = #{seq}
	</delete>
	
	
	
	<!-- 게시물 총 갯수 -->
	<select id="count" resultType="int">
		select count(seq) from board
	</select>
	
	
	
	<!-- 게시물 목록 + 페이징 -->
	<select id="listPage" parameterType="hashMap" resultType="com.my.findpet119.common.board.service.BoardVO">
		select
			seq, title, content, hit_cnt, create_userid
		from board
			order by seq desc
			limit #{displayPost}, #{postNum}
	</select>	
	
	
	
	<!-- 게시물 목록 + 페이징 + 검색 -->
	<select id="listPageSearch" parameterType="hashMap" resultType="com.my.findpet119.common.board.service.BoardVO">
		select
			seq, title, content, b_file, hit_cnt, create_userid
		from board
				
		<if test='searchType.equals("title")'>
			WHERE title LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("content")'>
			WHERE content LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("title_content")'>
			WHERE title LIKE concat('%', #{keyword}, '%') 
				or content LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("create_userid")'>
			WHERE create_userid LIKE concat('%', #{keyword}, '%')
		</if>
		
		order by seq desc
			limit #{displayPost}, #{postNum}
	</select>	
	
	<!-- 게시물 총 갯수 + 검색 적용 -->
	<select id="searchCount" parameterType="hashMap" resultType="int">
		select count(seq) from board		
			
		<if test='searchType.equals("title")'>
			WHERE title LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("content")'>
			WHERE content LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("title_content")'>
			WHERE title LIKE concat('%', #{keyword}, '%') 
				or content LIKE concat('%', #{keyword}, '%')
		</if>
		
		<if test='searchType.equals("creater_userid")'>
			WHERE create_userid LIKE concat('%', #{keyword}, '%')
		</if>
		
	</select>
	
	
</mapper>